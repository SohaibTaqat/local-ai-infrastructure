# Prometheus configuration
# Alloy pushes host/container metrics via remote write.
# File-based service discovery picks up model endpoints dynamically.

global:
  scrape_interval: 15s
  evaluation_interval: 15s
